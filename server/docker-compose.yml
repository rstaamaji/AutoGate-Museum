version: "3.8"

services:
  postgres:
    image: postgres:16
    container_name: autogate-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: autogatedb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - autogate

  backend:
    build: 
      context: ./backend
    container_name: autogate-server
    restart: unless-stopped
    depends_on:
      - postgres
    env_file:
      - ./backend/.env
    environment:
<<<<<<< Updated upstream:server/docker-compose.yml
      - DATABASE_URL=postgresql+psycopg2://postgres:password@postgres:5432/autogatedb
=======
      - DATABASE_URL=postgresql+psycopg2://postgres:root@postgres:5432/autogatemuseumdb

>>>>>>> Stashed changes:server/backend/docker-compose.yml
    ports:
      - "8000:8000"
    volumes:
      - ./backend/storage:/app/storage
    networks:
      - autogate

  frontend:
    build:
      context: ./frontend
    container_name: autogate-frontend
    restart: unless-stopped
    ports:
      - "5174:5174"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - VITE_API_URL=http://localhost:8000
    depends_on:
      - backend
    networks:
      - autogate

networks:
  autogate:
    driver: bridge

volumes:
  postgres_data:
